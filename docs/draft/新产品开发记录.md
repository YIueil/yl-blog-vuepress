# 新产品开发记录

## 1 前端-gs-widegets

## 2 后端-sgs-app-matter
### 2.1 Swagger地址
> http://localhost:6200/sgs-app-matter/swagger-ui/index.html
### 2.2 生成Q类
后端根目录执行
```sh
mvn clean install -U -X -E -DskipTests
mvn -U -pl sgs-customized/sgs-customized-core -am clean generate-sources -DskipTests
```
###  状态值
processstate、lifestate、f_status（好像0才是正常）。

## 3 数据库

## 4  本地测试

问题：
1、现在项目之间如何关联的？- 使用proejctRelation进行关联
2、

测试记录：
1. 已发送案件返回列表页面没有刷新状态。
2. 表单必填暂时没有配置。
3. 接件箱详情查看有两个页面。接件箱暂时没有未接件、已接件区分，需要增加对于已经接件的案件查询。![[Pasted image 20260716091317.png]]
4. 合卷功能：暂时没做，待开发。
5. 接件完成后，案件没有自动打开。
6. 审核材料确认， 没有确认按钮。
7. 会审意见：部门会审节点，每个人只展示自己所有的会审意见。按填报时间降序排列，并且现阶段填写的放到第一行。
8. 流程上的流程流转控制暂时没做。
9. 补正流程发起后，没有关闭当前页面，导致可以重复发起补正。
10. 撤销接件功能暂时没做。
11. 跨流程回退功能暂时没有做。
12. 审批流程暂时没有做。
13. 会审时，opinionModel默认为材料补正，意外触发了这个字段没有赋值，导致无法查询到opinion进行汇总。
14. 从经办人汇总意见回退都专班审查的时候，不能选择所有的会审人员，只能选择到最后一个会审人员。
15. 会审意见签字点快点，可以生成多条意见bug。
16. 不予受理功能待完善。
17. 回到补正专班审查时，优先展示会审意见填报。
